<%= form_with(model: pkmn, local: true) do |form| %>
  <% if pkmn.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pkmn.errors.count, "error") %> prohibited this pkmn from being saved:</h2>

      <ul>
      <% pkmn.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="row">
    <div class="form-group col-xs-4">
    <%= form.label :gen %>
    <%= form.text_field :gen, class: "form-control"%>
  </div>

  <div class="form-group col-xs-4">
    <%= form.label :name %>
    <%= form.text_field :name, class: "form-control"%>
  </div>
</div>

<div class="row">
    <div class="form-group col-xs-4">
    <%= form.label :typ1 %>
    <%= form.text_field :typ1, class: "form-control"%>
  </div>

  <div class="form-group col-xs-4">
    <%= form.label :typ2 %>
    <%= form.text_field :typ2, class: "form-control"%>
  </div>
</div>


  <div class="actions">
    <div class="form-group">
      <input type="submit" value="Send" class="btn btn-success" />
    </div>
  </div>
<% end %>
