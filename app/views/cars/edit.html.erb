
<h1>Editing Car</h1>

<%= render 'form', car: @car %>

<%= link_to raw('Zur&uuml;ck'), cars_path, class: "btn btn-danger"%>

<%= link_to 'Destroy', client, method: :delete, data: { confirm: 'Are you sure?' } %>

<%= link_to raw('Daten anzeigen'), @car, class: "btn btn-warning"%>